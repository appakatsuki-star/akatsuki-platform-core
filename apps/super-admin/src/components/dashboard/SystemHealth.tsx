import type { AdminLocale } from "../../localization/admin-translations";import { health } from "./mission-control-data";
export default function SystemHealth({locale}:{locale:AdminLocale}){return <article className="mission-panel health-panel"><header className="mission-panel-head"><div><span>INFRASTRUCTURE</span><h3>{locale==="ar"?"صحة النظام":"System Health"}</h3></div><b><i/>{locale==="ar"?"تشغيلي":"Operational"}</b></header><div className="health-grid">{health.map(item=><div key={item.name}><span><i/>{item.name}</span><strong>{item.ms}</strong><small><i style={{width:`${item.level}%`}}/></small></div>)}</div></article>}
