import type { AdminLocale } from "../../localization/admin-translations";import { launches } from "./mission-control-data";
export default function QuickLaunch({locale,onNavigate}:{locale:AdminLocale;onNavigate:(id:string)=>void}){return <article className="mission-panel quick-launch"><header className="mission-panel-head"><div><span>LAUNCHPAD</span><h3>{locale==="ar"?"تشغيل سريع":"Quick Launch"}</h3></div></header><div>{launches.map(item=><button type="button" onClick={()=>onNavigate(item.target)} key={item.en}><i>{item.icon}</i><span>{locale==="ar"?item.ar:item.en}</span></button>)}</div></article>}
