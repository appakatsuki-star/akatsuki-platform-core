import type { AdminLocale } from "../../localization/admin-translations";import AppIcon from "../ui/AppIcon";import { kpis } from "./mission-control-data";
const icons=["commerce","clients","products","users","revenue","orders","ai","providers"];
export default function KpiGrid({locale}:{locale:AdminLocale}){return <section className="mission-kpi-grid" aria-label={locale==="ar"?"مؤشرات الأداء":"Performance indicators"}>{kpis.map((item,index)=><article className="mission-kpi" tabIndex={0} key={item.en} style={{"--delay":`${index*35}ms`} as React.CSSProperties}><header><i><AppIcon name={icons[index]}/></i><b>{item.change}</b></header><strong>{item.value}</strong><span>{locale==="ar"?item.ar:item.en}</span><small>{locale==="ar"?"مقارنة بالفترة السابقة":"vs. previous period"}</small><svg viewBox="0 0 120 38" aria-hidden="true"><polyline points={item.points.map((p,i)=>`${i*20},${38-p*.65}`).join(" ")}/></svg></article>)}</section>}
