import type { AdminLocale } from "../../localization/admin-translations";
export interface FactoryFilters{query:string;type:string;status:string;owner:string;sort:string}
export default function ProjectFilters({locale,value,onChange}:{locale:AdminLocale;value:FactoryFilters;onChange:(value:FactoryFilters)=>void}){const set=(key:keyof FactoryFilters,next:string)=>onChange({...value,[key]:next});return <section className="factory-filters"><label className="factory-search"><span>⌕</span><input value={value.query} onChange={e=>set("query",e.target.value)} placeholder={locale==="ar"?"ابحث عن مشروع أو عميل...":"Search project or client..."}/></label><Select value={value.type} onChange={v=>set("type",v)} label={locale==="ar"?"كل الأنواع":"All types"} options={["Digital Commerce","Gaming Marketplace","SMM Panel"]}/><Select value={value.status} onChange={v=>set("status",v)} label={locale==="ar"?"كل الحالات":"All statuses"} options={["draft","active","review"]}/><Select value={value.owner} onChange={v=>set("owner",v)} label={locale==="ar"?"كل المسؤولين":"All owners"} options={["Omar N.","Lina K.","Sami R."]}/><Select value={value.sort} onChange={v=>set("sort",v)} label={locale==="ar"?"آخر تحديث":"Recently updated"} options={["name"]}/></section>}
function Select({value,onChange,label,options}:{value:string;onChange:(v:string)=>void;label:string;options:string[]}){return <select value={value} onChange={e=>onChange(e.target.value)} aria-label={label}><option value="">{label}</option>{options.map(option=><option value={option} key={option}>{option}</option>)}</select>}
