import type { AdminLocale } from "../../localization/admin-translations";import type { FactoryProject } from "../../types/project-factory";import ProjectStatusBadge from "./ProjectStatusBadge";
export default function ProjectCard({project,locale,onOpen}:{project:FactoryProject;locale:AdminLocale;onOpen:()=>void}){return <article className="factory-project-card"><header><div className="factory-project-mark">{project.name.slice(0,2).toUpperCase()}</div><div><h3>{project.name}</h3><p>{project.client}</p></div><ProjectStatusBadge status={project.status} locale={locale}/></header><dl><div><dt>{locale==="ar"?"النوع":"Type"}</dt><dd>{project.type}</dd></div><div><dt>{locale==="ar"?"القالب":"Template"}</dt><dd>{project.template}</dd></div><div><dt>{locale==="ar"?"النطاق":"Domain"}</dt><dd dir="ltr">{project.domain}</dd></div><div><dt>{locale==="ar"?"المسؤول":"Owner"}</dt><dd>{project.owner}</dd></div></dl><div className="factory-platforms">{project.platforms.map(platform=><span key={platform}>{platform}</span>)}</div><footer><small>{locale==="ar"?`أُنشئ ${project.created} · حُدث ${project.updated}`:`Created ${project.created} · Updated ${project.updated}`}</small><div><button type="button" aria-label={locale==="ar"?"إجراءات المشروع":"Project actions"}>•••</button><button type="button" onClick={onOpen}>{locale==="ar"?"فتح":"Open"} ←</button></div></footer></article>}
