import type { AdminLocale } from "../../localization/admin-translations";import AppIcon from "../ui/AppIcon";
export default function ProjectTypeCard({item,selected,locale,onSelect}:{item:{id:string;icon:string;name:string;ar:string;description:string;best:string;modules:number;featured?:boolean};selected:boolean;locale:AdminLocale;onSelect:()=>void}){return <button type="button" className={`project-type-card ${selected?"selected":""}`} aria-pressed={selected} onClick={onSelect}><i><AppIcon name={item.icon}/></i>{item.featured&&<b>{locale==="ar"?"موصى به":"Featured"}</b>}<h3>{locale==="ar"?item.ar:item.name}</h3><p>{item.description}</p><small>{locale==="ar"?"الأنسب لـ":"Best for"}: {item.best}</small><span>{item.modules} {locale==="ar"?"وحدة متاحة":"available modules"}</span></button>}
