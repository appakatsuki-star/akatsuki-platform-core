import { pick,ui,type AdminLocale } from "../../localization/admin-translations";
import SidebarNavigation from "./SidebarNavigation";
export default function AdminSidebar({activeId,expandedId,collapsed,mobileOpen,locale,onExpand,onNavigate,onCollapse,onClose}:{activeId:string;expandedId:string|null;collapsed:boolean;mobileOpen:boolean;locale:AdminLocale;onExpand:(id:string)=>void;onNavigate:(id:string)=>void;onCollapse:()=>void;onClose:()=>void}){return <><button className={`drawer-overlay ${mobileOpen?"is-visible":""}`} type="button" aria-label={pick(ui.closeMenu,locale)} onClick={onClose}/><aside className={`admin-sidebar ${collapsed?"is-collapsed":""} ${mobileOpen?"is-mobile-open":""}`}><header className="sidebar-brand"><img src="/assets/akatsuki-logo.png" alt=""/><div><strong>Akatsuki Platform</strong><span>Super Admin</span></div><button type="button" className="sidebar-collapse" onClick={onCollapse} aria-label={pick(collapsed?ui.expand:ui.collapse,locale)}>{collapsed?(locale==="ar"?"‹":"›"):(locale==="ar"?"›":"‹")}</button></header><div className="sidebar-scroll"><SidebarNavigation activeId={activeId} expandedId={expandedId} collapsed={collapsed} locale={locale} onExpand={onExpand} onNavigate={onNavigate}/></div><footer className="sidebar-status"><i/><div><span>{pick(ui.environment,locale)}</span><strong>{pick(ui.internal,locale)}</strong></div></footer></aside></>}
