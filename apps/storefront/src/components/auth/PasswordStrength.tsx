import type { Locale } from "../../types/storefront";
export function PasswordStrength({ password, locale }: { password: string; locale: Locale }) { const score = [password.length >= 8, /[A-Z]/.test(password), /[a-z]/.test(password), /\d/.test(password)].filter(Boolean).length; const level = score <= 1 ? "weak" : score <= 3 ? "medium" : "strong"; const label = { weak: locale === "ar" ? "ضعيفة" : "Weak", medium: locale === "ar" ? "متوسطة" : "Medium", strong: locale === "ar" ? "قوية" : "Strong" }[level]; return <div className={`password-strength strength-${level}`}><div><span /><span /><span /></div><p><strong>{locale === "ar" ? "قوة كلمة المرور:" : "Password strength:"} {label}</strong><small>{locale === "ar" ? "8 أحرف مع حرف كبير وصغير ورقم" : "8 characters with upper, lower, and a number"}</small></p></div>; }
