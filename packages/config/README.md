# Config Package Placeholder

**Status:** Placeholder only. No production code or dependencies exist yet.

## Future responsibility

This package will provide framework-neutral, typed, fail-closed configuration contracts approved under `FND-003`. It will describe configuration metadata and validation, not store secret values.

## Not allowed yet

No source directory, schema implementation, environment file, real value, secret, browser-unsafe export, dependency, build configuration, or runtime command may be added under the current approval.
